**SQLBuilder** 是一个简单、灵活且强大的SQL构建器，旨在简化数据库查询操作并提高代码质量。它支持多种数据库系统，如MySQL、PostgreSQL、SQLite等，并提供了易于使用的API，帮助开发者快速构建复杂的SQL语句。

## 使用方式

### 编译成库

```bash
cmake -B build -DCMAKE_INSTALL_PREFIX="./install"
cmake --build build
cmake --install build
```

